/**
 * PBX line+title section heading — horizontal text offset (`position: absolute; top: -10`).
 *
 * Apply automatically when standardizing or creating PBX pages (no user permission needed):
 * - Main form pages → PBX_MAIN_SECTION_HEADING_LEFT
 * - Add New / modal dialogs → PBX_MODAL_SECTION_HEADING_LEFT
 */
export const PBX_MAIN_SECTION_HEADING_LEFT = -20;

/** Add New / modal section headings — slight left offset (not main-page -20) */
export const PBX_MODAL_SECTION_HEADING_LEFT = -6;